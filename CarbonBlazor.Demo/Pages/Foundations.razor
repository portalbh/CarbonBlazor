@page "/foundations"

<PageTitle>Foundations</PageTitle>

<CbBreadcrumb>
    <CbBreadcrumbItem Href="/">Overview</CbBreadcrumbItem>
    <CbBreadcrumbItem Current="true">Foundations</CbBreadcrumbItem>
</CbBreadcrumb>

<h1>Foundations</h1>
<p class="docs-lede">CarbonBlazor uses role-based tokens for color, spacing, layer, type, focus, support, and motion. Themes change values without changing component markup.</p>

<section class="docs-section">
    <h2>Themes</h2>
    <div class="docs-theme-row">
        @foreach (var theme in Enum.GetValues<CbTheme>())
        {
            <CbThemeProvider Theme="@theme" Class="docs-theme-swatch">
                <strong>@theme</strong>
                <span>Layer and text tokens</span>
            </CbThemeProvider>
        }
    </div>
</section>

<section class="docs-section">
    <h2>Dark Theme Token Comparison</h2>
    <div class="docs-token-compare">
        <CbThemeProvider Theme="CbTheme.White" Class="docs-token-panel">
            <strong>White</strong>
            <CbButton>Primary action</CbButton>
            <CbTextInput Label="Field token" Value="White field" />
            <CbLink Href="#">Link token</CbLink>
        </CbThemeProvider>
        <CbThemeProvider Theme="CbTheme.G100" Class="docs-token-panel">
            <strong>G100</strong>
            <CbButton>Primary action</CbButton>
            <CbTextInput Label="Field token" Value="G100 field" />
            <CbLink Href="#">Link token</CbLink>
        </CbThemeProvider>
    </div>
</section>

<section class="docs-section">
    <h2>Layering</h2>
    <CbLayer Level="CbLayerLevel.One" Class="docs-layer-demo">
        <p>Layer 1</p>
        <CbLayer Level="CbLayerLevel.Two" Class="docs-layer-demo">
            <p>Layer 2</p>
        </CbLayer>
    </CbLayer>
</section>
